[package]
name = "fission"
version = "0.11.1"
edition = "2021"
license = "MIT"
repository = "https://github.com/fission-ui/fission"
description = "Production-focused Rust application framework for macOS, Windows, Linux, Web, Android, iOS, Terminal, Static site, and SSR targets"
categories = ["gui", "rendering", "visualization", "wasm", "web-programming"]
keywords = ["fission", "ui-framework", "gpu-accelerated", "cross-platform", "vello"]

[package.metadata.fission]
platforms = ["android", "ios", "linux", "macos", "windows", "web", "terminal", "static-site", "ssr"]

homepage = "https://fission.rs"
documentation = "https://fission.rs"
readme = "README.md"
[features]
default = []
charts = ["dep:fission-charts"]
desktop = ["dep:fission-shell-desktop", "interactive-canvas"]
desktop-tray = ["desktop", "fission-shell-desktop?/tray"]
icons-reflection = ["fission-icons/reflection"]
interactive-canvas = [
    "fission-core/interactive-canvas",
    "fission-widgets/interactive-canvas",
]
ios = ["dep:fission-shell-mobile", "interactive-canvas"]
android = ["dep:fission-shell-mobile", "interactive-canvas"]
mobile = ["dep:fission-shell-mobile", "interactive-canvas"]
platform-shells = [
    "desktop",
    "mobile",
    "web",
]
site = ["dep:fission-shell-site"]
server = ["dep:fission-shell-server"]
server-actix = ["server", "fission-shell-server?/actix-adapter"]
server-axum = ["server", "fission-shell-server?/axum-adapter"]
server-redis = ["server", "fission-shell-server?/redis"]
terminal-shell = ["dep:fission-shell-terminal"]
terminal-widget = ["fission-widgets/terminal"]
test-driver = ["dep:fission-test-driver"]
three-d = [
    "dep:fission-3d",
    "fission-shell-desktop?/three-d",
    "fission-shell-mobile?/three-d",
    "fission-shell-web?/three-d",
]
video = [
    "fission-shell-desktop?/video",
    "fission-shell-mobile?/video",
    "fission-shell-web?/video",
]
web = ["dep:fission-shell-web", "interactive-canvas"]

[dependencies]
fission-core = { path = "../../core/fission-core", version = "0.11.1" }
fission-3d = { path = "../../core/fission-3d", version = "0.11.1", optional = true }
fission-ir = { path = "../../core/fission-ir", version = "0.11.1" }
fission-layout = { path = "../../core/fission-layout", version = "0.11.1" }
fission-text-engine = { path = "../../core/fission-text-engine", version = "0.11.1" }
fission-theme = { path = "../../core/fission-theme", version = "0.11.1" }
fission-i18n = { path = "../../core/fission-i18n", version = "0.11.1" }
fission-widgets = { path = "../fission-widgets", version = "0.11.1" }
fission-macros = { path = "../fission-macros", version = "0.11.1" }
fission-icons = { path = "../fission-icons", version = "0.11.1" }
fission-charts = { path = "../fission-charts", version = "0.11.1", optional = true }
fission-render = { path = "../../rendering/fission-render", version = "0.11.1" }
fission-diagnostics = { path = "../../tools/fission-diagnostics", version = "0.11.1", default-features = false }
fission-shell-site = { path = "../../shell/fission-shell-site", version = "0.11.1", optional = true }
fission-shell-server = { path = "../../shell/fission-shell-server", version = "0.11.1", optional = true }
fission-shell-terminal = { path = "../../shell/fission-shell-terminal", version = "0.11.1", optional = true }
fission-test-driver = { path = "../../tools/fission-test-driver", version = "0.11.1", optional = true }
serde = { version = "1.0", features = ["derive"] }

[target.'cfg(not(any(target_os = "android", target_os = "ios", target_arch = "wasm32")))'.dependencies]
fission-shell-desktop = { path = "../../shell/fission-shell-desktop", version = "0.11.1", optional = true }

[target.'cfg(any(target_os = "android", target_os = "ios"))'.dependencies]
fission-shell-mobile = { path = "../../shell/fission-shell-mobile", version = "0.11.1", optional = true }

[target.'cfg(target_arch = "wasm32")'.dependencies]
fission-shell-web = { path = "../../shell/fission-shell-web", version = "0.11.1", optional = true }
